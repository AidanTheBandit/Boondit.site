---
/**
 * Flash Status Component
 * Displays connection status for WebUSB, Web Serial, Bootloader, and Fastbootd
 */
---

<article class="card">
  <h2 class="card-title">Status</h2>
  <div class="space-y-3">
    <div class="status-row">
      <span>WebUSB</span>
      <span id="webusbStatus" class="status-pill">Checking...</span>
    </div>
    <div class="status-row">
      <span>Web Serial</span>
      <span id="webserialStatus" class="status-pill">Checking...</span>
    </div>
    <div class="status-row">
      <span>Bootloader</span>
      <span id="bootloaderStatus" class="status-pill">Not connected</span>
    </div>
    <div class="status-row">
      <span>Fastbootd</span>
      <span id="fastbootdStatus" class="status-pill">Not connected</span>
    </div>
  </div>
</article>

<style>
  .card {
    background: rgba(31, 41, 55, 0.6);
    backdrop-filter: blur(8px);
    border-radius: 1rem;
    padding: 2rem;
    border: 1px solid rgba(75, 85, 99, 0.5);
  }

  .card-title {
    font-size: 1.375rem;
    font-weight: 700;
    color: #fff;
    margin-bottom: 1rem;
    padding-bottom: 0.75rem;
    border-bottom: 2px solid rgba(168, 85, 247, 0.3);
  }

  .status-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-size: 0.9rem;
    color: #cbd5e1;
  }

  .status-pill {
    font-size: 0.75rem;
    padding: 0.25rem 0.6rem;
    border-radius: 999px;
    background: rgba(148, 163, 184, 0.2);
    color: #94a3b8;
  }

  :global(.status-pill.is-ok) {
    background: rgba(34, 197, 94, 0.2);
    color: #86efac;
  }

  :global(.status-pill.is-error) {
    background: rgba(248, 113, 113, 0.2);
    color: #fecaca;
  }
</style>
